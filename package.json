{
  "name": "bean-book",
  "version": "0.1.0",
  "description": "Untappd for coffee beans — a static, offline PWA that showcases coffee bean reviews sourced from GitHub Issues.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-data.js && node scripts/build-site.js",
    "start": "node scripts/serve.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.9",
    "axe-core": "4.12.1",
    "jsdom": "29.1.1",
    "vitest": "4.1.9"
  }
}
